# 2026-09-02 工作日志

## Mymq-rs 学习文档衔接审查
- 用户完成 LEARNING.md 阶段 1，询问阶段 2~5 课程位置：阶段 2 = LEARNING-2-quic.md（已存在）；阶段 3~5 仅路线图，无教程正文。
- 完成 LEARNING.md → LEARNING-2-quic.md 衔接审查，报告落盘 `LEARNING-bridge-review.md`。
- 主要发现：
  1. 🔴 缺口：超时重投（redelivery_worker）未接入阶段 2 网络层（协议无 Redeliver 命令、server.rs 无巡检 worker），需在 server.rs 补 2 行巡检任务。
  2. 🟡 缺口：Notify 等待唤醒机制无网络映射，网络版为纯轮询 DEQUEUE（pull 模型），文档未言明。
  3. 🟡 缺口：步骤一"demo 函数可保留"有可见性坑（wait_for_message 访问 Broker 私有字段 topics/notifier，main.rs 变 bin 后无法编译）；2 个旧测试需搬进 broker.rs，文档未指示。
  4. 🟢 用户代码偏差：next_message_id 先自增再返回导致 id 从 2 开始（文档示例输出 MSG 1 对不上）；subscribe 返回 &mut（文档版返回 ()）。
- 用户代码现状：src/main.rs 单文件，含全部核心方法 + 2 测试 + rand 依赖（demo 用）；Cargo.toml 无 [lib]，tokio 缺 net/io-util feature。
